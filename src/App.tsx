import { FC } from 'react';
import StartPage from './pages/StartPage/StartPage';

const App: FC = () => {
  return (
    <section className="App">
      <StartPage />
    </section>
  );
}

export default App;
